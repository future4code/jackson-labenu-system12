export type Teacher = {
    id: string,
    name: string,
    email: string,
    birthDate: string,
    mission: string
};

export type Student = {
    id: string,
    name: string,
    email: string,
    birth_date: string,
    mission: string
}